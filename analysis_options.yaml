include: package:solvro_config/app.yaml

plugins:
  riverpod_lint:
    version: ^3.1.3
    diagnostics:
      provider_dependencies: false # this works weirdly
  solvro_config:
    version: ^1.8.0
    diagnostics:
      provider_dependencies: false # this works weirdly
